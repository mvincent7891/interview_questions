// Cracking the Coding Interview
// Chapter 6: Math and Logic Puzzles

export const sieveOfEratosthenes = max => {
  // starting with 3, scan through all odd numbers below max and see if
  // they are divisible by 3. If they are, cross them off as primes.
  // When you hit the first number not divisible by 3, record it as your
  // next prime. Repeat the process with this next prime until either
  // there are no more primes or you reach max. 
};
